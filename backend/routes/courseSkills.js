const express = require('express');
const router = express.Router();
const courseSkillController = require('../controllers/courseSkillController');

// Example course skill routes (expand as needed)
// router.get('/', courseSkillController.getAllCourseSkills);
// router.get('/:id', courseSkillController.getCourseSkillById);
// ...other course skill routes (add, update, remove, etc.)

module.exports = router;
