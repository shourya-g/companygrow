const express = require('express');
const router = express.Router();
const courseEnrollmentController = require('../controllers/courseEnrollmentController');

// Example course enrollment routes (expand as needed)
// router.get('/', courseEnrollmentController.getAllCourseEnrollments);
// router.get('/:id', courseEnrollmentController.getCourseEnrollmentById);
// ...other course enrollment routes (enroll, unenroll, progress, complete, etc.)

module.exports = router;
